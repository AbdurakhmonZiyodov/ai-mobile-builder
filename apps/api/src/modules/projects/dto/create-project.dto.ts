import { createProjectInput, type CreateProjectInput } from "@amb/contracts";

/**
 * Yangi loyiha so'rovi.
 *
 * Sxema `@amb/contracts` da — frontend ham aynan shuni ishlatadi, shuning
 * uchun maydon nomi o'zgarsa ikkala tomon bir vaqtda buziladi va xato
 * kompilyatsiyada topiladi.
 */
export { createProjectInput };
export type CreateProjectDto = CreateProjectInput;
